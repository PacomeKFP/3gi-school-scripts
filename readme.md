# Computers Architechture
===

## C codes compilations commands:

>- In prod mode with gcc: **`gcc src/main.c lib/*.c -o app -I include`**
>- In dev mode with gcc: **`gcc src/main.c lib/*.c -o bin/app -I include`**
