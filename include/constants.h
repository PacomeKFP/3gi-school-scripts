#ifndef _CONSTANTS_H
#define _CONSTANTS_H

#define SCAN_STR_REG "%[^\n]s"
#define FILE_EXISTS(file, rights) access(file, rights)

// ChildProcess *all_processes = (ChildProcess *)malloc(sizeof(ChildProcess));
// int number_of_processes = 0;

#endif